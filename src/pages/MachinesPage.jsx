import usePageTitle from '../hooks/usePageTitle.js'

export default function MachinesPage() {
  usePageTitle('Machines')
  return (
    <section className="section">
      <div className="container">
        <h1>Machines — under construction</h1>
      </div>
    </section>
  )
}
